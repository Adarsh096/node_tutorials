// generate the package.json file:
//npm init --yes

// install packages locally
// npm -i <packagename>
//npm install <packagename>


// install packages globally
// npm install -g <globally>
// Use npx instead of the global installations
//$ npx <your command here>
//$ npx nodemon app.js

// npm creates a folder node_modules wehen we install the dependencies or modules.

// we can uninstall packages by $ npm uninstall <packageName>

// package-lock.json  ==> It has the version info of all the packages which are directly and indirectly being used in the project.

