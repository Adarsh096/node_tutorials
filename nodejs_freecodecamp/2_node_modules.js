//modules : breakdown the big app into separate modules handling separate things.
// modules : encapsulated code

const secret = 'XCAP8443jcc70Oslkj';
const john = 'john';
const peter = 'peter';

const sayHi = (myName)=>{
    console.log(`hello there ${myName}`)
};
sayHi('susan');
sayHi(john);
sayHi(peter);

// same thing we can acheive by placing the names in separate file(module) and then importing and accessing them in this file :

//now require() the exported values
const names = require('./2_b_node_modules')
console.log(names)



