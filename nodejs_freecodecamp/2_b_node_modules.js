// contains the names to be imported :

const tanya = 'tanya';
const somesh = 'somesh';
const secretvar = 'super secret value'
// now we need to export these values:
// just dump the values to be exported in the exports{} object
module.exports = {
    tanya,
    somesh // this syntax allows to pass just the key since the value is the same as the key
}



